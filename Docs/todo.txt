**Addressing modes**

Round 1:
- [ ok] IMP — implied
- [ ok] IMM — immediate
- [ok] ZP0 — zero page
- [ok] ABS — absolute

Round 2:
- [ ok] ZPX — zero page, X
- [ ok] ZPY — zero page, Y
- [ok ] ABX — absolute, X (+1 cycle on page cross)
- [ ok] ABY — absolute, Y (+1 cycle on page cross)
- [ok ] REL — relative (branches only)

Round 3:
- [ok ] IND — indirect (JMP only)
- [ ok] IZX — (indirect, X)
- [ok ] IZY — (indirect), Y (+1 cycle on page cross)

---

**Instructions**

Load / store:
- [ ok] LDA — load accumulator (page penalty: yes)
- [ok ] STA — store accumulator (page penalty: no)
- [ok] LDX — load X (page penalty: yes)
- [ok ] STX — store X (page penalty: no)
- [ok ] LDY — load Y (page penalty: yes)
- [ ok] STY — store Y (page penalty: no)

Transfers:
- [ok ] TAX — A → X
- [ ok] TXA — X → A
- [ ok] TAY — A → Y
- [ ok] TYA — Y → A
- [ok ] TXS — X → SP
- [ ok] TSX — SP → X

Jump / subroutine:
- [ok ] JMP — jump
- [ok] JSR — jump to subroutine  <---
- [ ok] RTS — return from subroutine <---

Logical:
- [ok] AND (page penalty: yes)
- [ok] ORA (page penalty: yes)
- [ok] EOR (page penalty: yes)

Arithmetic:
- [ ] ADC — add with carry (page penalty: yes)
- [ ] SBC — subtract with carry (page penalty: yes)
- [ ] INC — increment memory (page penalty: no)
- [ ] DEC — decrement memory (page penalty: no)
- [ ] INX — increment X
- [ ] DEX — decrement X
- [ ] INY — increment Y
- [ ] DEY — decrement Y

Compare:
- [ok] CMP — compare A (page penalty: yes)
- [ok] CPX — compare X
- [ok] CPY — compare Y

Branches (all use REL, no page penalty):
- [ ] BEQ — branch if Z set
- [ ] BNE — branch if Z clear
- [ ] BCS — branch if C set
- [ ] BCC — branch if C clear
- [ ] BMI — branch if N set
- [ ] BPL — branch if N clear
- [ ] BVS — branch if V set
- [ ] BVC — branch if V clear

Stack:
- [ok] PHA — push A
- [ok] PLA — pull A
- [ok] PHP — push status
- [ok] PLP — pull status

Flags:
- [ok ] SEC — set carry
- [ ok] CLC — clear carry
- [ ok] SEI — set interrupt disable
- [ ok] CLI — clear interrupt disable
- [ ok] SED — set decimal
- [ ok] CLD — clear decimal
- [ ok] CLV — clear overflow

Rotate
ASL
LSR
ROL
ROR
BIT
BRk
RTI
Misc:
- [ok] NOP